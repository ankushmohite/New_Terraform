#################### How to Install Terraform ##########################################

suao apt update -y
wget -O - https://apt.releases.hashicorp.com/gpg | sudo gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(grep -oP '(?<=UBUNTU_CODENAME=).*' /etc/os-release || lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
sudo apt update && sudo apt install terraform

<block> <parameter> {
    arguments
}

* block 
If i want create infra then i use. ( block = resource, same  for block = output, block = variable )
If i want to show output by using terraform the i use block = output.

block         parameters
--------     --------------
resource      aws_instance my-instance {

}

##################################################################################
vi main.tf
resource local_file my_file {
filename = "automate.txt"
content = "best contain for devops"

}


Terraform init --> Initialised the terraform 


